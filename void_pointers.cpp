#include<iostream>
using namespace std;

int main()
{
    // char a_val = 'c';
    // float b_val = 2.41;
    // int c_val = 34;
    // void* ptr[] = {(void*)&a_val, (void*)&b_val, (void*)&c_val};
    // cout<<*((float*)ptr[0])<<' '<<*((float*)ptr[1])<<' '<<*((float*)ptr[2])<<'\n';
    cout<<"manik\rgarg";
}